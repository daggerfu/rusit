[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rusit"
version = "0.1.0"
description = "Utilities for the RUSIT 3.0 analytic Russian-based language"
requires-python = ">=3.10"

[project.scripts]
rusit = "rusit.cli:main"
